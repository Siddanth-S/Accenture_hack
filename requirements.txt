# Core — this is all you need to run the prototype
pydantic>=2.6
pyyaml>=6.0
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
httpx>=0.27
python-multipart>=0.0.9

# Production session store (optional — set PREFLIGHT_REDIS_URL to activate;
# the store degrades to in-memory with a warning if this is absent/unreachable)
# redis>=5.0

# Optional NLI entailment for groundedness (Stage 2). Set PREFLIGHT_NLI_MODEL
# to activate; without these the engine uses the documented lexical fallback.
# sentence-transformers>=2.2
# torch>=2.0

# Stage 3 / further detectors (optional — engine degrades explicitly without them)
# onnxruntime>=1.17
# presidio-analyzer>=2.2
# opentelemetry-sdk>=1.24
